# Leave empty when the production web server proxies /api to the backend.
# Otherwise use the publicly reachable API origin, without a trailing slash.
# Example: VITE_API_BASE_URL=https://api.example.com
VITE_API_BASE_URL=
